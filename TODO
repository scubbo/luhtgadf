* Figure out why the `wait` function is required (and remove it if possible!)
* Change behaviour based on whether cookies exist (set up "install" and "run" behaviour)

Notes
=====

install.py 

setup.py (--no-gui) (--message <message>) <email> <password>
(logs in, saves cookies
